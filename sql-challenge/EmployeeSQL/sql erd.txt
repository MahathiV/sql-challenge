department
-
dept_no varchar pk fk -< dept_emp.dept_no fk -< dept_manager.dept_no
dept_name varchar

dept_emp
-
emp_no varchar 
dept_no varchar 
from_date date
to_date date

dept_manager
-
dept_no varchar  
emp_no varchar 
from_date date
to_date date

employees
-
emp_no varchar pk fk -< titles.emp_no fk -< salaries.emp_no fk -< dept_emp.emp_no fk -< dept_manager.emp_no
birth_date date
first_name varchar
last_name varchar
gender varchar(10)
hire_date date

salaries
-
emp_no varchar
salary bigint
from_date date
to_date date

titles
-
emp_no varchar
title varchar
from_date date
to_date date